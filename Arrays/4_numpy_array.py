import numpy as np

import numpy as np

arr1 = np.array([1, 2, 3, 4])
arr2 = np.zeros(5)          # Array of zeros
arr3 = np.ones(5)           # Array of ones
arr4 = np.arange(1, 10, 2)  # Array with step size


# Mathematical operations
print(arr1 + 2)    # Add 2 to each element
print(arr1*3)    # Multiply each element by 3

# Slicing and indexing
print(arr1[1:3])   # Slice array [2, 3]

